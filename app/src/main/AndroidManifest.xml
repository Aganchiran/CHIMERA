<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chimera">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".chimerafront.activities.CommingSoonActivity"></activity>
        <activity
            android:name=".chimerafront.activities.MainMenuActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".chimerafront.activities.CharacterListActivity"
            android:launchMode="singleTop" />
        <activity android:name=".chimerafront.activities.CharacterDetailsActivity" />
        <activity android:name=".chimerafront.activities.AddCharacterActivity" />
    </application>

</manifest>