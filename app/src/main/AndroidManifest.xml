<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aganchiran.chimera">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.aganchiran.chimera.chimerafront.activities.MainMenuActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.aganchiran.chimera.chimerafront.activities.CharacterProfileActivity"
            android:configChanges="orientation|screenSize" />
        <activity android:name="com.aganchiran.chimera.chimerafront.activities.CommingSoonActivity" />
        <activity android:name="com.aganchiran.chimera.chimerafront.activities.CharacterListActivity"
            android:label="@string/characters"
            android:launchMode="singleTop" />
        <activity android:name="com.aganchiran.chimera.chimerafront.activities.AddEditCharacterActivity" />
    </application>

</manifest>